ML_SERVICE_NAME=ics-ndr-engine
APP_ENV=development
ML_RANDOM_STATE=42
# When set, all endpoints except /healthz require X-ML-Internal-Key header (backend sends this)
ML_INTERNAL_API_KEY=
# Comma-separated origins if browser access is ever required (normally empty — backend-only)
ML_CORS_ORIGINS=
